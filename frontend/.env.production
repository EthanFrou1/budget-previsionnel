# Placeholder - replace with the real deployed Api URL before building for production
# (Lot 16). Left unset, the client falls back to http://localhost:5080, which is wrong
# outside local dev.
VITE_API_BASE_URL=https://REPLACE-ME.example.com
